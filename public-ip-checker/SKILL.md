---
name: Check Public IP
description: Gets the machine's current public IP address.
---
# Public IP Checker Skill

This skill allows you to look up the current public IP address of the machine executing the script using the ipify API.

## How to use

Run the provided script to get the IP address.

```bash
node scripts/check-ip.js
```

The script will output the IP address.
